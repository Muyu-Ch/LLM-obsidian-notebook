---
tags:
  - AI/语言模型
---

之前说[[数据集]]就是我们希望ai学会的东西
但是机器本质只能处理数字
所以我们采用了映射表的方法来处理[[数据集]]里的元素
比如对于棋盘上的每一个位置，我们可以用坐标表示
在bigram里，我们使用字符与数字一一对应

映射表的作用就是把世间的万事万物转化为数字给ai理解
并且也可以用反向的映射表把ai输出的数字再转化为我们需要的有意义的东西

## 但"一个数字"还不够

这里说的映射，是把一个东西变成**一个**数字（比如字符 `a` 对应 1）
可这样的数字会带来一个问题：[[神经网络]]会把它当成真正的数值来算，于是 `a`(1) 和 `b`(2) 就天然比 `a`(1) 和 `z`(26) 更近
而这个远近是我们排字母表排出来的，不是数据说的

[[独热]]是绕开这件事的第一招，[[嵌入层]]则更进一步：**把"一个数字"升级成"一串数字"（向量）**，让远近由[[训练]]自己决定