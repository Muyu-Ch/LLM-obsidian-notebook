---
tags:
  - AI/训练
---

鲁棒性是 robust 的音译（也译作"稳健性"），意思就是**抗造**、结实、不容易坏

放到模型身上：**输入有点变化，它还能给出正确的答案。**

## 反面就是"脆弱"

[[过拟合]]的模型就是脆弱的。

它在训练集上一分不丢，可输入稍微一变就崩——因为按[[噪声]]那一篇的说法，它背的是"**橘色**"，不是"**猫**"。橘色只在训练集那个特定环境里成立，一换光照、一换花色，它的判断就塌了。

所以：

> **一个模型的鲁棒性，就是它抵抗"输入被扰动"的能力。**

## 和泛化的区别

这两个词经常被混着用，但它们问的问题不太一样：

| | 泛化 | 鲁棒性 |
|---|---|---|
| 面对的是 | **没见过的新样本** | 见过的样本**被改动之后** |
| 像考试里 | 换一道新题 | 同一道题，字迹歪了、印糊了、换了个说法 |
| 对立面 | 没学到规律（欠拟合/过拟合） | 一扰动就输出离谱答案 |

- **泛化**关心的是"换新题还会不会做"
- **鲁棒性**关心的是"题目还是那道题，但样子变了还会不会做"

不过实战里两者常常一起出现——数据被扰动之后，对模型来说其实也算一种"没见过"。**可以理解成：鲁棒性是泛化的一个更硬的版本。**

## 为什么模型会脆弱

还是那个根源：**模型学的是"训练集里出现过的所有关联"，包括巧合。**

而巧合有个要命的性质——**它只在原来的环境下成立。**

- 训练集里猫都是橘色的 → "橘色"和"猫"绑定了
- 训练集里所有句子都是某种句式 → 模型把句式也当成规律
- 这些关联在训练集上**全都成立**，所以 [[loss]] 很低，看不出任何问题

一旦环境变了，这些关联就集体失效。模型不是"答错一点"，而是**整套判断依据都塌了**。

**靠着会变的东西做出的判断，就是脆弱的。**

## 怎么让它变鲁棒

回头看，你已经学过的招数其实**全都在干同一件事**：

| 手段 | 怎么提升鲁棒性 |
|---|---|
| 噪声 / 数据增强 | 把"会变的东西"提前变一遍，逼模型去找不变的部分 |
| [[正则化]]（权重衰减） | 让函数变平缓，输入小变化不引起输出大波动 |
| [[BatchNorm]] | 按 batch 统计量归一化，顺带注入噪声 |
| Dropout | 不让模型依赖任何单个[[神经元]] |
| 更多、更杂的数据 | 让"会变的东西"自然出现在数据里 |

共同点就一句：**都在拆掉模型对"会变的东西"的依赖。**

## 平滑就是鲁棒

这一点在[[正则化]]里推过：权重衰减会把 W 压小，而 $y = Wx + b$ 里 **W 越小，输入 x 变一点点，输出就只变一点点**。

**这就是鲁棒性的数学表达——输入的小扰动不会引起输出的大波动。**

所以"让模型平滑"和"让模型鲁棒"是同一件事的两种说法。这也是为什么 [[BatchNorm]] 顺带能提升鲁棒性：它让 hpreact 集中在一个稳定的区间里，不会因为输入的一点变化就冲到饱和区去。

## 在你的 makemore 里，鲁棒性是从哪来的

有个地方特别值得回头看——**嵌入层**。

- **bigram 的计数表完全不鲁棒**：见过就是见过，没见过就是 0 次。输入稍微偏一点，它直接傻掉
- **[[嵌入层]]让它变了**：字符被表示成向量之后，"相似的字符有相似的向量"，所以模型见到一个没怎么见过的组合，也能给出**合理**的答案

这正是[[嵌入层]]那一篇里说的那句话：

> 训练集里出现一句话，涨的不只是这一句话的概率，还有它在向量空间里**一整片"邻居"**的概率。

**那片"邻居"就是鲁棒性。**

输入扰动一点 → 查出来的向量只动一点 → 后面的计算只动一点 → 输出的[[概率]]只动一点。整条链路上没有哪一步会突然崩掉。

## 一个值得知道的边界：对抗样本

鲁棒性**不是一个单一的旋钮**。

有这样一种现象：一张模型认得百分百正确的图，只要给它加上**人眼完全看不出来的、精心构造的一点点扰动**，模型就会信誓旦旦地把它识别成完全不同的东西。这类被做出来的输入叫**对抗样本**。

它说明模型的鲁棒性是**有方向性的**：

- 对**自然的**扰动（光照、噪声、模糊）可能很鲁棒
- 对**专门冲着它去的**扰动却很脆弱

所以"这个模型鲁棒吗"这个问题，得先问清楚：**对着什么扰动的鲁棒？**
（顺带一提，这也提示了一件事：模型判断的依据和我们以为的，可能并不是同一套。）

## 代价：鲁棒不是免费的

鲁棒性和"锐度"往往是**互相拉扯**的：

- 你让它对扰动不敏感，它也可能对**真正有意义的细节**变得不敏感
- 就像[[正则化]]里说的：拿训练集表现换泛化——这里可以延伸成**拿一点"反应灵敏"换"稳"**

压得太狠，模型就变成了对什么都点头的老好人；压得太松，它又会被一点点风吹草动带偏。

## 总结

| 问题 | 答案 |
|---|---|
| 鲁棒性是什么 | 输入被扰动之后，模型还能不能给出正确答案 |
| 反面是什么 | 脆弱——判断依据是"会变的东西"，一变就塌 |
| 和泛化什么区别 | 泛化看"没见过的新样本"，鲁棒性看"见过的东西变了样" |
| 为什么模型会脆弱 | 它把训练集里那些**恰好成立**的巧合也当成了规律 |
| 怎么提升 | 加噪声、正则化、BatchNorm、Dropout、更多更杂的数据——都在拆掉对"会变的东西"的依赖 |
| 数学上是什么 | 平滑——输入的小扰动不引起输出的大波动 |
| makemore 里从哪来 | [[嵌入层]]：相似的输入有相似的向量，所以没见过也能给出合理的答案 |
| 有什么边界 | 对抗样本说明鲁棒性是有方向的——得先问"对着什么扰动" |
| 代价是什么 | 太鲁棒会变得对真正的细节也不敏感 |

一句话：**鲁棒性就是让模型的判断建立在"不会变的东西"上——因为建立在会变的东西上的判断，迟早会塌。**
