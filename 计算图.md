---
tags:
  - AI/数学
---

[[前向传播]]里说要"记录运算"，但记录运算到底记的是什么？为什么要记？

记录的这张东西就叫**计算图**。

## 每个节点记了三件事

每次发生一次运算（加、乘、tanh……），我们不只是算出一个数字，还顺便记下三件事：

| 记的东西 | 代码里叫什么 | 是什么 |
|---|---|---|
| 我算出来是多少 | `data` | 一个数字 |
| 我是从谁算出来的 | `_prev` | 参与这次运算的输入 |
| 我的结果变一点，输入该怎么变 | `_backward` | 局部求导规则 |

看加法长什么样：

```python
def __add__(self, other):
    out = Value(self.data + other.data, (self, other), '+')
    def _backward():
        self.grad += 1.0 * out.grad
        other.grad += 1.0 * out.grad
    out._backward = _backward
    return out
```

`out` 这个节点记住了"我是 a 和 b 加出来的"，并且随身带着一条规则：**我变多少，a 和 b 就各变多少。**

把每个这样的节点用线连起来，就是一张图。这张图就是计算图。

## 局部导数：每个节点只管自己那一小步

注意 `_backward` 里全是 `out.grad`——它根本不知道 [[loss]] 是什么，也不知道自己在网络的第几层。

它只知道一件事：**我的输出变一点点，我的输入该变多少。**

这正是 [[链式法则]] 里那句话的意思——"可以完全不关心 f(x) 的内部是什么样的，只要把数值拿过来就可以了"。每个节点把自己的那一小步交出来，剩下的交给 [[反向传播]] 去连乘。

## 在图上倒着走一遍

反向传播就是从 loss 出发，在计算图上倒着走。每走到一个节点，就调用它的 `_backward`，把梯度分给它的输入。

拿一个具体的例子：$\text{loss} = (a-b)^2$，其中 $a=3$、$b=1$。

计算图长这样：

```
Value(-1) ──┐
            ├── ( × ) ──► nb = -1 ──┐
b = 1 ──────┘                       │
                                    ├── ( + ) ──► c = 2 ── ( **2 ) ──► loss = 4
a = 3 ──────────────────────────────┘
```

前向传播算出来的数是：$nb = -1$、$c = 2$、$\text{loss} = 4$。

现在倒着走：

1. `loss.grad = 1.0` —— 起点，自己对自己的导数是 1
2. 走到 `( **2 )`：$\dfrac{d(c^2)}{dc} = 2c = 4$ → `c.grad = 4`
3. 走到 `( + )`：加法导数恒为 1，原样分发给两个输入 → `a.grad = 4`、`nb.grad = 4`
4. 走到 `( × )`：$\dfrac{d(b \times -1)}{db} = -1$ → `b.grad = -4`

结果：$\partial \text{loss}/\partial a = 4$，$\partial \text{loss}/\partial b = -4$。

和解析解对得上：$\partial \text{loss}/\partial a = 2(a-b) = 4$，$\partial \text{loss}/\partial b = -2(a-b) = -4$。

## 图的形状取决于代码怎么写

上面那张图里，`a - b` 并不是一个"减法节点"。

因为我们代码里 `__sub__` 写的是 `self + (-other)`，所以数学上的一步减法，在图上被拆成了两步：先 $b \times -1$，再相加。

**计算图记录的是代码的实际计算过程，不是数学公式的样子。** 同一个数学表达式，实现方式不同，图就不一样——这也是为什么 [[前向传播]] 必须把每一步都记下来，而不是只记最后结果。

## 梯度累加：为什么是 `+=` 而不是 `=`

看一个只有三行、但很关键的例子：

```python
x = Value(3.0)
y = x * x
y.backward()
print(x.grad)    # 6.0
```

x 参与了两次乘法，而且两次是**同一个** x：

```
       ┌─────┐
x ────►│     │
       │  ×  ├────► y = 9
x ────►│     │
       └─────┘
   ↑
 这两根线是同一个 x
```

回到乘法的 `_backward`：

```python
self.grad  += other.data * out.grad
other.grad += self.data  * out.grad
```

因为 `self` 和 `other` 是同一个对象，两行都落在 $x$ 身上，各加 3：

$$x.\text{grad} = 3 + 3 = 6$$

而 $\dfrac{d(x^2)}{dx} = 2x = 6$，对上了。

**如果这里写的是 `=` 而不是 `+=`，第二行会把第一行覆盖掉，`x.grad` 就变成 3 而不是 6——错的。**

这不是个刁钻的例子。在真实的 [[神经网络]] 里，**几乎每个 [[参数]] 都会通过多条路径影响 loss**：比如你 MLP 那 4 个训练样本，每一个都要用同一批权重算一遍，loss 又是四项之和，所以每个权重都会收到四份梯度。少累加一份，训练就是错的。

## 拓扑排序：为什么不能排着队走

`_backward` 读的是 `out.grad`，这就要求：**轮到某个节点时，它的所有"下游"必须已经把梯度传完了。**

如果不排序，一个被多条路径共享的节点可能在 `out.grad` 还没收全的时候就被调用，算出来的梯度是错的，而且这个错的会被继续往上传——一路污染到最前面。

所以 `backward()` 第一步是拓扑排序：

```python
def build_topo(v):
    if v not in visited:
        visited.add(v)
        for child in v._prev:
            build_topo(child)     # 先递归到输入那一头
        topo.append(v)            # 输入先入列，结果后入列
```

它保证一件事：**任何节点都排在它所有的输入之后。**

```
topo      : [ a, b, ..., nb, ..., c, ..., loss ]
             └─── 输入 ───┘        └─── 结果 ───┘

reversed  : [ loss, ..., c, ..., nb, ..., b, a ]
             └─── 从结果出发，一路走回输入 ───┘
```

（同一层的先后顺序无所谓，梯度累加本来就是可交换的。）

这样还有个额外好处：**每个节点的 `_backward` 只会跑一次。** 不排序的话，共享的子图会被重复计算，层数一多就是指数级的浪费。

## 回头看

到这里，[[前向传播]] 里那句"记录运算"就完整了：

| | 干什么 | 对应代码 |
|---|---|---|
| [[前向传播]] | 算出值，顺便把图建起来 | `data`、`_prev`、`_backward` |
| [[反向传播]] | 在图上倒着走，分发梯度 | `backward()` 里的 `reversed(topo)` |
| [[链式法则]] | 保证局部导数连乘起来是对的 | 每个 `_backward` 里的 `* out.grad` |
| [[梯度消失]] | 连乘时只要有一环接近 0，整条链就衰减 | —— |

一句话：**前向传播建图，反向传播遍历图，链式法则决定边上怎么传。**
