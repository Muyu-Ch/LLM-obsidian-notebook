---
tags:
  - AI/训练
---

## 一个反直觉的操作

[[训练]]的时候，我们恨不得数据越干净越好
但有一类技巧恰恰相反：**故意往训练过程里掺噪声**，效果反而更好

[[BatchNorm]] 就是一个例子——它带来的噪声，正是它顺带能[[正则化]]的原因

## 噪声为什么有用：猫的例子

假设我们要训练一个识别"猫"的模型

如果训练集里**每一张图片都是橘猫**，那模型很可能把"橘色"这个属性和猫绑在一起——它学到的是"橘色 = 猫"，而不是"猫长什么样"

这时候如果加入噪声：**花色在变、环境在变、背景在变、光照在变……**

这些无关的变量一直在变，模型就**没法靠记住它们来把 loss 压低**（因为下一次它们又不一样了）。于是它被迫去找那些**在所有噪声下都不变**的东西：

四条腿、长尾巴、尖耳朵、长胡须……

> **噪声做的事，是让"死记硬背"变得不划算。**

死记硬背的前提是"东西每次都一样"。一旦无关变量每次都在变，背它们就等于白费力气，模型只能被逼着去找那些**真正稳定、真正属于"猫"的特征**。

## 换个说法：噪声打散了"虚假的关联"

- 训练集里"橘色"和"猫"同时出现了很多次，这**在数据里是真的**，但它不是我们想学的规律
- 模型没有能力分辨"这是规律"还是"这只是这批数据恰好如此"——它只看 [[loss]]
- 噪声的作用就是把这些巧合**一次次打散**：如果一个关联在加了噪声之后依然成立，那它大概率是真的

这正好接上[[过拟合]]那一篇：过拟合就是模型把训练集里的巧合也当成规律背了下来。**噪声是对症下药。**

## 噪声从哪来

| 来源 | 噪声是什么 |
|---|---|
| 数据增强 | 翻转 / 裁剪 / 变色——人为制造"同一个东西的不同样子" |
| [[BatchNorm]] | 每个样本的归一化要用同 batch 其他样本的统计量，对其他样本来说这就是扰动 |
| Dropout | 训练时随机关掉一部分[[神经元]]，每次"看到的网络"都不一样 |
| Mini-batch | 每次只拿一小撮数据算梯度，梯度本身就在抖 |

最后一条特别值得注意：**随机梯度下降本身就一直带着噪声**。每次只用一小批数据估出来的 [[grad]]，方向和真实梯度总有偏差——这可能是所有噪声里最基础的一种。

## 但噪声不是越多越好

- **噪声太小** → 打不散那些巧合，该背的还是照背
- **噪声太大** → 连真正的规律也被打散了，模型什么都学不到（就滑向欠拟合了）

所以噪声强度和[[正则化]]里的 $\alpha$ 一样，又是个**旋钮**。

## 和正则化的关系

[[正则化]]那一篇说：正则化的本质是**限制模型迎合训练集的自由度**。噪声是其中一条路，但它的下手位置不太一样：

| | 从哪儿下手 | 具体做法 |
|---|---|---|
| 权重衰减 | **参数侧** | 要求 W 尽量小 |
| 噪声 | **数据侧** | 让训练数据不那么"好记" |

两条路的终点是同一个：**让模型对输入的小扰动不敏感**——也就是变"平滑"，变[[鲁棒性]]。

而"平滑"正好同时缓解了两件事：[[过拟合]]（不再死抠训练集里的巧合）和饱和（不会把某几个输入推到极端的响应上去）。

## 总结

| 问题 | 答案 |
|---|---|
| 为什么故意加噪声反而更好 | 它让"死记硬背"变得不划算，逼模型去找真正的特征 |
| 噪声到底打散了什么 | 训练集里那些**恰好成立**的虚假关联 |
| 常见来源 | 数据增强、BatchNorm、Dropout、mini-batch 采样 |
| 和正则化什么关系 | 噪声是"从数据侧"下手的一种正则化，权重衰减是"从参数侧"下手 |
| 什么时候过头 | 太大连真规律也打散，会变成欠拟合 |
| 共同的好处 | 让模型平滑——既不容易过拟合，也不容易饱和 |

一句话：**噪声逼着模型去学"不管怎么扰动都不变"的东西，而那个东西通常才是真正的特征。**
