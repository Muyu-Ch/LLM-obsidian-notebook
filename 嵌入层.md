---
tags:
  - AI/语言模型
---

[[映射表]]里说，要让ai理解世界就得先把世界变成数字。[[token]]里说，要把语言切成有意义的单位，再建立映射。

到这里为止，每个token都拿到了一个**编号**：`.` = 0，`a` = 1，`b` = 2……

但编号是个陷阱。

## 问题一：编号的远近是假的

编号确实是个数字，可它不该被当数字用。

`a`=1、`b`=2，这不代表 b 是 a 的两倍——这句好懂。真正麻烦的是更隐蔽的那一层：

[[神经网络]]的第一层是 $y = Wx + b$（见[[神经元]]）。它拿到一个数，就会**当成一个数**来算：编号 5 和 6 天生离得近，5 和 25 天生离得远。

可这个远近是我们随手排的。代码里 `chars = sorted(list(set(...)))` 只是把26个字母按字母表顺序排了一遍。凭什么 `e` 和 `f` 在网络里就该挨着？凭什么 `a` 和 `z` 就该离得最远？

> **字符之间该有什么样的关系，应该由数据说了算，不该由我们排字母表的顺序说了算。**

论文里（Bengio 2003）的说法更狠：在离散空间里，随便改动一个离散变量的取值，都可能让函数值剧烈变化，结果就是"大多数观察到的对象，彼此之间在**汉明距离**上几乎是最大程度的远"——翻译过来就是：**所有人跟所有人都一样陌生。**

这样的输入喂进去，网络永远学不到"这两个字符有点像"这种信息。

## 问题二：数表和独热都撑不住

先看数表。bigram 的做法是直接数：27×27 = 729 个格子，数出来就完事了（见[[似然]]里说的，计数表就是最大似然估计）。

但这是假设我们只看1个字符。看3个字符，可能的上下文就有 27³ = 19683 种；看10个字符，是 27¹⁰ ≈ 2×10¹⁴ 种。而我们的[[数据集]]一共才三万个名字——**格子还没数完，就已经远远多于数据了。**

（论文里用的是10万个词的词表，算下来是 $100000^{10} - 1 = 10^{50} - 1$ 个自由参数。他们给这件事起了个名字，叫**维数灾难**。）

再看[[独热]]。makemore_1 里已经用过：

```python
xenc = F.one_hot(xs, num_classes=27).float()
```

编号 13 变成一个27维向量，第13位是1，其余26位全是0。

它解决了"假的关系"：27根轴互不相干，谁跟谁都没有天然距离。但代价是**它把关系清成了零**——任意两个独热向量都正交（内积为0、夹角90度）。我们不再替数据表态"e 和 f 有点像"，可我们也没给网络任何**能力**去发现这件事。

所以两条路都堵死了：

- 直接喂编号 → 塞进去一堆假的关系
- 老实数表 → 参数量指数爆炸，而且没有泛化

**嵌入层是同时解决这两个问题的那一招。**

## 独热其实已经在查表了

先回头看一眼 makemore_1 里那行代码：

```python
logits = xenc @ W      # W 是 (27, 27)
```

这个矩阵乘法在数学上干的事，就是**把 W 的第13行原样取出来**。因为除了第13位，其他26位全是0，乘出来全是0，加到最后只剩那一行。

也就是说，我们一直以为在做"矩阵乘法"，其实一直在做"**查表**"。

那么问题来了：既然结果就是"取出某一行"，我们为什么还要真的去做这个乘法？

27维里只有1个1，剩下26维全是0。拿一个几乎全是0的向量去乘一个矩阵，绝大部分计算是在算"0乘以一个数，再加上去"——**等于什么都没干。**

所以直接查表：

```python
C = torch.randn((27, 40))   # 一张表: 27行, 每行40个数
C[3]                        # 'c' 的向量: 直接取出第3行
C[torch.tensor([5,6,7])]    # 一次取三行
emb = C[X]                  # X 是 (32, 3) 的编号, emb 是 (32, 3, 40)
```

**这就是嵌入层。**

它和"独热 @ 矩阵"是同一件事的两种写法，只是一个老老实实做乘法，一个直接取行。笔记本里那行验证就是这么写的：

```python
F.one_hot(torch.tensor(5), 27).float() @ C      # 结果和 C[5] 完全一样
```

关键要记住的是：**C 不是查出来的，C 是学出来的。** 它就是那张"每个token对应一串数字"的表，和[[映射表]]里说的一样——只不过表里的内容不再是数出来的次数，而是要[[训练]]出来的[[参数]]。

## 为什么它可以是一层"层"

论文里管这个叫 **word features layer**，也就是网络的第一层。

对照 [[神经元]]：一层网络的组成是 $y = Wx + b$。嵌入层就是其中的 $Wx$——**没有 b，也没有[[激活函数]]**。

为什么没有 b？b 的作用是给所有输入统一加一个偏移。可这里我们要的是"把某一行的全部数值原样取出来"，加 b 会让所有字符的向量一起平移，没有任何意义。

为什么没有激活函数？因为这一层干的事是**查表**，不含任何需要被"复杂化"的非线性变换。论文的原话是：这一层没有非线性，"加了也没什么用"（*the shared word features layer C, which has no nonlinearity (it would not add anything useful)*）。

它是整个网络里**唯一一层在"查找"而不是在"计算"的层**。查完之后，向量才交给隐藏层和[[激活函数]]去算。

## 它是参数，所以它会自己长出结构

体现在代码里就是这么一行：

```python
parameters = [C] + Ws + bs
```

C 和 W、b 平起平坐，`requires_grad = True`，一起被[[反向传播]]求 [[grad]]，一起被[[更新参数]]。

刚初始化的时候，`torch.randn` 把27个字符**乱撒**在一片空间里（笔记本里那张散点图，就是把2维的 C 画在平面直角坐标系上）。训练完之后再看，意思相近的字符会自己聚到一起——比如元音 a、e、i、o、u 会抱团。

**这个"相近"不是我们规定的，是数据压出来的。** 我们只规定了"每个字符用几个数表示"，剩下的全交给 [[loss]] 下降。

这就是嵌入层真正值钱的地方，也是论文里那段著名的类比：

> 如果 dog 和 cat 扮演的角色相似，the 和 a 相似，bedroom 和 room 相似，is 和 was 相似，running 和 walking 相似……那么"**The cat is walking in the bedroom**"这句话，会自然地让"**A dog was running in a room**"也变得合理。

因为相似的东西有相似的向量，而概率函数是这些向量的**光滑函数**——输入动一点点，输出就只动一点点。

所以训练集里出现一句话，涨的不只是这一句话的[[概率]]，还有它在向量空间里**一整片"邻居"**的[[概率]]。论文的摘要把这件事说得最清楚：

> **让每一句训练句子，都在告诉模型关于"指数级数量的语义邻居"的信息。**

这是数表法做不到的：数表里，"cat"和"dog"是两个互不相干的格子，谁也不会因为见过前者而对后者多一分把握。

## 一张表要多宽：n_embd 是个旋钮

C 的形状是 (27, n_embd)：行数是词表大小（27个字符，固定的），**列数 n_embd 是我们自己定的**。

这个数就是"我们允许网络用几个数来描述一个字符"。2维的话可以直接画在平面上看（论文里也特意建议先取 m=2，"更容易画出来"），但2个数装不下26个字母之间的微妙关系；makemore_2 最后用的是 40 维。

维度越大，能记录的特征越多，要让网络学的东西也就越多。所以它和 block_size、lr 一样，是个"旋钮"。

顺带一提，这也是嵌入法在参数效率上赢过数表法的地方：**C 的大小 (27×n_embd) 跟上下文长度完全无关**，上下文变长只是"多查几次表"。数表法是 $27^{k}$，嵌入法是 $27 \times m$——一个指数，一个常数。

## 一次看3个字符：查完了还要拼起来

`block_size=3` 意味着一次要看3个字符。X 的形状是 (32, 3)，查完表 emb 的形状是 (32, 3, 40)。

但下一层要的是**一个**向量，不是3个。所以要把3个字符的向量首尾接起来：

```python
emb.view(-1, block_size * n_embd)      # (32, 120), 也就是 3 × 40
```

这解释了 `layer_size` 的第一个数为什么必须是 `block_size * n_embd`——它就是这个拼起来的向量的长度。论文里也是这么写的：

$$x = \big(C(w_{t-1}),\ C(w_{t-2}),\ \dots,\ C(w_{t-n+1})\big)$$

**注意三个位置查的是同一张 C。** 这就是论文说的"共享参数"：不管 'a' 出现在第一个位置还是第三个位置，它用的都是同一个向量。所以在一个位置上学会的东西，别的位置也能直接沾光。

## 一个有意思的性质：C 的梯度是稀疏的

W、b 这些参数，每一步都会被更新——因为每一步的数据都要穿过它们。

C 不一样。`C[X]` 只取出了 X 里出现的那几行，[[反向传播]]的梯度也就只落在**那几行**上。没出现在这个 minibatch 里的字符，它们的行收到的 [[grad]] 是 0，这一步原地不动。

论文里专门提了这一点：

> 每次更新后，**很大一部分参数并不需要被更新或访问**：那些没有出现在输入窗口里的词，它们对应的特征向量 $C(j)$。

这跟 [[计算图]] 里说的"梯度累加"是一回事：没有参与这次运算的东西，收不到任何梯度。只不过在 C 这里这个现象特别明显——**每个字符的向量，只在它出现的时候才学一点。**

## 回头看[[映射表]]

把几个阶段的"映射表"摆在一起看：

| | 一个token被映射成 | 表里的内容谁定的 | 字符之间的关系从哪来 |
|---|---|---|---|
| bigram 计数表 | 一个数字（次数） | 我们数出来的 | 数据统计出来的，但只能"一对一" |
| 独热向量 | 27个数，只有1个是1 | 我们排的 | 没有——刻意留空 |
| 嵌入层 C | m个数（向量） | **训练学出来的** | 数据自己长出来的 |

[[映射表]]说要把万事万物变成数字给ai理解。嵌入层把这件事又推进了一步：

前两种是"把一个东西变成**一个**数字"，嵌入层是"把一个东西变成**一串**数字"。

一串数字，才能表达出"它在哪些方面像什么"。这就是为什么 [[token]]、[[映射表]] 和嵌入层要放在一起看——**映射的方式，决定了ai能理解多少。**

## 总结

| 问题 | 答案 |
|---|---|
| 为什么不能直接喂编号 | 编号的远近是假的，网络会当真；而数表法参数量指数爆炸、又不会泛化 |
| 独热解决了什么 | 让每个字符互不相干，把"它们之间是什么关系"这个问题留空 |
| 但独热的问题 | 所有独热向量两两正交，等于把关系清成了零；而且一整串0乘矩阵纯属浪费 |
| 嵌入层是什么 | 一张 (词表大小 × n_embd) 的**参数表**，查表取出某个token的向量 |
| 它和独热什么关系 | 数学上等价：独热 @ C 就是取出 C 的某一行；直接查表省掉了那些0乘法 |
| 它在网络里的位置 | 第一层；没有 b，也没有激活函数 |
| 它怎么学 | 和别的参数一样：反向传播 + 更新参数；梯度只落在出现过的行上 |
| 学完长什么样 | 意思相近的token自己聚到一起——不是我们规定的，是数据压出来的 |
| 它值钱在哪 | 见过一句话，连带它在向量空间里的一整片"邻居"也变合理了（泛化） |
| n_embd 是什么 | "每个token用几个数来描述"，是个旋钮 |

一句话：**嵌入层把"编号"升级成了"坐标"，让字符之间的远近，由数据自己说了算。**
