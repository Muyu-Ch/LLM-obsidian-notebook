---
tags:
  - AI/训练
---

BatchNorm 是"批量归一化"（Batch Normalization）的缩写

## 问题：那一层的输出不能太大，也不能太小

makemore_3 里我们盯上了隐藏层里**过[[激活函数]]之前**的那一步

[[神经元]]算的是 $Wx + b$，但这个结果还不能直接往下传，要先过 tanh。我们把这个"过 tanh 之前"的值记作 hpreact

它有两个方向的毛病：

- **太小** → tanh 在 0 附近近似是一条直线，等于没有非线性。那这一层就白加了（见[[激活函数]]里说的：没有非线性，多层堆起来还是个线性函数）
- **太大** → tanh 进入两侧的平坦区，导数趋近 0，[[梯度消失]]，这一层就学不动了

所以理想状态是：**hpreact 最好近似服从标准正态分布**——集中在 0 附近，又不会跑太远。

（这和第 3 章前面讲的参数初始化是同一个目标。但初始化只能管**刚开始**那一下，训练一跑起来参数就动了，这个状态保不住）

## 那就直接归一化

论文里的想法很直接：

> 既然我们希望 hpreact 符合标准正态分布，**那我们为什么不直接把它归一化？**

$$x \to \frac{x - \text{mean}}{\text{std}}$$

- 减去均值 → 均值变成 0
- 除以标准差 → 标准差变成 1

这就是**批量归一化**。

## 但这样太强硬了，所以加两个旋钮

如果每一层都强行归一化，等于**硬性规定**每个线性层必须输出标准正态分布。这会极大地限制网络的自由度

所以论文加了一个平滑：归一化之后，再乘一个 `gain`、加一个 `bias`：

$$h_{\text{preact}} = \text{gain} \cdot \frac{x - \text{mean}}{\text{std}} + \text{bias}$$

这两个也是[[参数]]（初始化为 1 和 0），也会被[[训练]]：

- 网络想要标准正态，就让 gain = 1、bias = 0
- 网络觉得别的分布更合适，它可以自己把这两个数调走

> **我们希望模型往标准正态靠拢，但不强迫它。**

这是这一手最巧妙的地方——**限制是软的**。

## 一个反直觉的地方：batch 里的样本互相影响了

归一化用的 mean 和 std 是从哪来的？**是从这一批（batch）数据里现算的。**

这就带出一个很反直觉的后果：

原本，一个 batch 里的样本彼此**没有任何关系**——它们只是随机抽出来的，各算各的，只在最后求 loss 的时候取了个平均而已

但用了 BatchNorm 之后，**每个样本的归一化都要用到同 batch 其他样本的统计量**，于是它们在[[前向传播]]的时候就关联起来了。而且这个关联会一路带进[[反向传播]]。

> 一组本来无关的样本，被这一步绑在了一起。

## 而这个"副作用"恰恰是它有用的原因

回到[[噪声]]那一篇：对每个样本来说，**同 batch 的其他样本就是一种噪声**。

而且这个噪声每次都不一样（batch 是随机抽的），相当于给训练加了持续的随机扰动。于是：

- 模型没法死记硬背——它连"这次归一化会怎么变"都算不准
- 它被逼着去找那些**真正稳定的特征**
- 于是 BatchNorm **顺带**就具有了[[正则化]]的作用，能减轻[[过拟合]]

另外还有一个直接的好处：归一化让 hpreact 总是落在 0 附近，**不容易跑到 tanh 的平坦区**，所以饱和的问题也减轻了。

**这一步貌似不符合"神经网络"的意义（样本之间本该是独立的），但它偏偏极大地改善了网络。**

## 但别指望它立刻见效

makemore_3 里实测下来，loss 提升并不明显。原因很简单：**我们的隐藏层只有一层。**

层数这么浅，"怎么初始化"本来就是件容易的事，随便调调缩放就够了

BatchNorm 的价值要到**深层网络**才体现得出来：层数一多，信号一层层传下去，越传越大或越传越小几乎无法避免，这时候"每层都归一化一下"就从锦上添花变成了刚需。

## 总结

| 问题 | 答案 |
|---|---|
| 它要解决什么 | hpreact 太小时 tanh 退化成线性，太大时 tanh 饱和、梯度消失 |
| 怎么做的 | 减均值、除标准差，让它近似标准正态分布 |
| 为什么要加 gain 和 bias | 硬性归一化太限制自由度，这两个参数把控制权还给网络 |
| 反直觉在哪 | 同 batch 的样本本来无关，归一化让它们在前向传播里就互相影响了 |
| 为什么能正则化 | 归一化依赖 batch 统计量，对其他样本来说就是[[噪声]]，逼模型去找真特征 |
| 还有什么好处 | hpreact 总在 0 附近，不容易饱和 |
| 什么时候才值得用 | 深层网络；浅层网络靠初始化就够了 |

一句话：**BatchNorm 的本意是让信号别跑偏（不饱和、不消失），但它按 batch 统计量归一化的做法顺带引入了[[噪声]]，于是又白捡了一个[[正则化]]的效果。**
