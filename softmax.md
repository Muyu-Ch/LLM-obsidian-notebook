---
tags:
  - AI/数学
---

## 为什么需要它

softmax的作用就是把[[logits]]转化为真正的[[概率]]
[[神经网络]]有个缺陷：他本身是简单的计算，但[[概率]]的要求是要[[归一化]]：也就是所有东西加起来是1，而且全都落在0-1里面
如果在[[神经网络]]里硬加上这个条件就很复杂而且束手束脚
所以我们的策略是：让[[神经网络]]自由的去算浮点数，不给它添加[[归一化]]的约束，这样算出来的就是[[logits]]
然后我们根据这个自由的浮点数进行[[归一化]]
同时也把这个[[归一化]]的过程纳入[[链式法则]]里

## 怎么算：先取exp，再归一化

具体的计算过程是：先取exp，随后[[归一化]]
exp可以让数字全部落在正数区间
然后[[归一化]]
这样我们就得到了可以用的[[概率]]

$$\text{softmax}(z_i) = \frac{e^{z_i}}{\sum_{j=1}^{n} e^{z_j}}$$

（$z$ 就是[[logits]]那一串原始分数，$n$ 是选项的个数）

## 为什么偏偏是exp

并且exp还有个很好用的地方：它可以和[[loss]]里的ln计算相呼应，这会在[[反向传播]]的时候提供极大的便利
