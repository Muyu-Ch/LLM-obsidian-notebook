---
tags:
  - AI/数学
---

链式法则其实是微积分里的知识，这里用来求梯度[[grad]]
首先什么是链式法则，就是当一个函数里有多步计算时候求导的方式

## 最简单的比如

$g(x) = 10x$
$f(x) = x^2$
$h(x) = g(f(x))$

这个时候 $h(x)$ 对 $x$ 求导
那就是先对 $g(x)$ 求导，再对括号内求导
最后结果就是

$$h'(x) = g'(f(x)) \cdot f'(x) = 10 \cdot 2x = 20x$$

## 关键：不用管内层有多复杂

这个时候我们会发现：如果当我们知道了内层的 $f(x)$ 的梯度
那就可以直接套用来求外层的 $h(x)$ 的梯度
并且可以完全不关心 $f(x)$ 的内部是什么样的，是有多复杂
只要把数值拿过来就可以了

## 在神经网络里

[[神经网络]]本质就是
[[loss]] $= f(g(h(w(\dots)\dots)\dots))$
而在这个过程中，我们可以根据每一步的计算去一步步推导出[[loss]]关于每一个变量的偏导
