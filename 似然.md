---
tags:
  - AI/数学
---

似然是 [[loss]] 里"负对数似然"那四个字中间的俩字
但几乎所有教程提到它都是一句话带过，默认你已经懂了
这篇把它到底在问什么讲清楚

## 字面意思：似然就是"有多像"

likelihood 直译过来大概是"像话的程度"，中文翻成"似然"，也就是"像不像"

它问的问题很朴素：

> 我手上这组[[参数]]，有多"像"是产生这份数据的那个真实原因？

注意这里有个东西是钉死的：**数据已经发生了**，你改不了它
所以似然永远是在问：给定这份数据，哪组[[参数]]最像话

## 和概率的区别：同一个公式，问的方向反了

这是最容易绕晕的地方，因为它俩**的计算式长得一模一样**

设数据叫 $x$，[[参数]]叫 $\theta$：

$$P(x \mid \theta) \qquad \text{和} \qquad L(\theta \mid x)$$

前一个是[[概率]]，后一个是似然，符号都是一个竖线加两边
区别在于：**谁是钉死的，谁是变化的**

| | [[概率]] | 似然 |
|---|---|---|
| 钉死的是 | [[参数]] $\theta$ | 数据 $x$ |
| 变化的是 | 数据 $x$ | [[参数]] $\theta$ |
| 问的是 | 用这组参数，会生成出什么样的数据 | 用这份数据，最像由哪组参数生成 |
| 记号 | $P(x \mid \theta)$ | $L(\theta \mid x)$ |
| 要不要[[归一化]] | 要，$\sum_x P(x \mid \theta) = 1$ | 不要，$\sum_\theta L(\theta \mid x)$ 是多少都行 |

一句话：**概率是"从参数推到数据"，似然是"从数据倒推参数"**

## 它为什么不算概率

因为似然压根不满足[[归一化]]

数据定死之后，[[参数]]是可以随便取的：可以试 $(0.6, 0.2, 0.2)$，可以试 $(0.2, 0.4, 0.4)$，可以试无穷多组
每一组都对应一个似然值，而这些值加起来**不会是 1**，也没有任何理由要是 1

所以似然虽然名字里带个"然"，长得也像[[概率]]，但它不是概率
它只是**给每组参数打的一个分**

## 在 makemore 里，到底谁在问谁

回想[[训练]]的流程：手上有一份[[数据集]]（一堆"前一个字符 → 后一个字符"），要找出[[参数]]

- **概率**方向的问法：假如计数表已经长这样了，那下一个字符会是什么？—— 这是**生成**，是训完模型之后干的事
- **似然**方向的问法：我手上已经有一堆真实文本了，那什么样的计数表最像话？—— 这是**训练**

所以训练干的活就是：**在参数空间里翻找，找那组让训练数据似然最高的参数**

这个"找最高的"有个正经名字：**最大似然估计（MLE, Maximum Likelihood Estimation）**

## 为什么最后变成了"负对数似然"

从"最大似然"到"负对数似然"，中间动了两步，这两步是**独立**的

### 第一步：取 ln —— 解决连乘

[[loss]] 那边说过，整张卷子的似然是所有 $p_i$ 连乘起来的
很多个 0-1 之间的小数乘在一起，会小到浮点数表示不了，直接变成 0
取 ln 之后连乘变连加，数值就稳当了

而且这样做**不会改变答案**。因为 ln 是单调递增的：$a > b$ 等价于 $\ln a > \ln b$
所以"似然最大的那组参数"和"对数似然最大的那组参数"，**是同一组**
只是数值从 $0.00864$ 变成了 $-4.7514$（下面的例子）

### 第二步：加负号 —— 解决方向

我们想要的 [[grad]] 下降是"往下走"，也就是找**最小值**
但似然是越大越好，方向是反的
所以前面加个负号，把"最大化似然"翻译成"最小化负对数似然"

于是就有了 [[loss]] 里的那个式子：

$$\text{loss} = -\frac{1}{N}\sum_{i=1}^{N}\ln p_i$$

（再除以 $N$，是为了让 loss 不受样本数量影响）

## 自己算一遍

假设数据里，"e"后面出现过：m 三次、a 一次、t 一次，一共 5 次
现在有三组候选[[参数]]，看哪个最"像"：

| 候选参数 | 似然 | 对数似然 | NLL（就是 loss） |
|---|---|---|---|
| A $(0.6,\ 0.2,\ 0.2)$ —— 计数比例 | 0.0086400 | -4.7514 | **0.9503** |
| B $(0.2,\ 0.4,\ 0.4)$ —— 猜反了 | 0.0012800 | -6.6609 | 1.3322 |
| C $(0.8,\ 0.1,\ 0.1)$ —— 过度自信 | 0.0051200 | -5.2746 | 1.0549 |

```python
import math

n = {'m': 3, 'a': 1, 't': 1}        # 观察到的数据
N = sum(n.values())                  # 5

p = {'m': 0.6, 'a': 0.2, 't': 0.2}   # 候选参数

likelihood = math.prod(p[k] ** v for k, v in n.items())        # 0.00864000
log_lik    = sum(v * math.log(p[k]) for k, v in n.items())     # -4.7514
nll        = -log_lik / N                                      # 0.9503
```

两个结论：

1. A 胜出，而且 A 正好就是**数出来的比例** $(3/5,\ 1/5,\ 1/5)$
2. C 明明把最常出现的 m 押得更重（0.8 > 0.6），却输给了 A

第 2 点值得停一下。C 输在**过度自信**：它给 a 和 t 只留了 0.1，可 a 和 t 是真实出现过的
对数似然里 $\ln(0.1) \approx -2.30$，比 $\ln(0.2) \approx -1.61$ 差了一大截
而"猜中 m 三次"多赚的那点优势，补不上这个窟窿

这就是 [[loss]] 里说的"错的越离谱，惩罚力度越高"

## 意外收获：makemore_1 的计数表，就是 MLE

把上面这个例子推到底，结论是：让似然最大的那组[[参数]]，永远就是**观测到的频率**

$$p_i = \frac{n_i}{N}$$

也就是说，makemore_1 里那个"数一数、归一化"的计数表，**不是个凑合的起点，它就是最大似然估计的解析解**
（不信可以搜一遍参数空间，搜出来的最大值点和 $(0.6,\ 0.2,\ 0.2)$ 完全重合）

这件事顺带解释了 makemore_1 和 makemore_2 的关系：

- makemore_1 的 bigram 计数表，是**直接算出来的** MLE —— 因为模型太简单，有闭式解
- makemore_2 上了[[神经网络]]，参数太多算不出闭式解，只能一遍遍[[前向传播]] + [[反向传播]] + [[更新参数]]，**用梯度下降去逼近同一个目标**
- 两条路的终点是同一个：让训练数据的似然最大

所以"[[训练]]"这个词在这里也说得通了 —— 训练就是在找"最能解释这份数据"的那组参数

## 总结

| 问题 | 答案 |
|---|---|
| 似然是什么 | 给定数据，某组[[参数]]有多"像"是它的来源 |
| 和[[概率]]什么关系 | 公式一样，但钉死的和变化的对调了 |
| 它是[[概率]]吗 | 不是，不满足[[归一化]] |
| 最大似然估计在干嘛 | 挑让似然最高的那组参数 |
| 为什么要取 ln | 连乘变连加，且不改变最优解（单调递增） |
| 为什么要加负号 | 把"最大化"翻成"最小化"，好配合 [[grad]] 下降 |
| 计数表为什么好用 | 它就是 MLE 的解析解 |
