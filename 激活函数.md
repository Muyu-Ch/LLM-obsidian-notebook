---
tags:
  - AI/基础
---

[[神经元]]是 $y = Wx + b$
但是多个[[神经元]]不能直接堆叠
为什么？

## 堆叠起来会退化成一个线性函数

比如：

$y_1 = W_1x_1 + b_1$
$y_2 = W_2y_1 + b_2$

这个时候会发现：

$y_2 = W_2(W_1x_1 + b_1) + b_2$

一整理最后 $y_2$ 变成了一个关于 $x_1$ 的线性方程
结果就是多层加起来变成了一个单纯的线性函数
所以这样的[[神经网络]]会有个致命的问题：它天生不可能复杂，再怎么堆砌[[神经元]]最后就是一个非常简单的线性函数

## 所以要加非线性

所以我们要在[[神经元]]之间添加激活函数
这个激活函数没有别的作用
就是要让这个[[神经网络]]变得“复杂”
否则这个网络规模再怎么大终究就是个简单的线性

这个激活函数，就是在原本的线性计算上，添加上非线性元素
这类函数有，ReLU，tanh等等
