---
tags:
  - AI/训练
---

[[训练]]的目标是让 [[loss]] 变小
但如果只盯着这一件事，会走到一个很尴尬的地方：
**loss 越小，模型可能反而越没用**

## 问题：背题机器

makemore_1 里就见过这个苗头——训练到后面，W 逐渐逼近理论最优解，loss 稳定在 2.46 再也下不去

而如果我们换个更大的网络接着练，loss 还能更低。低到接近 0 的时候意味着什么？

> 模型只会吐出正确答案，成为了背题机器而不是学习机器。

这句话的关键在于：**我们要的从来不是"在见过的题上全对"。**

回到 [[数据集]] 里说的，我们要的是泛化——**没见过的新题也能做对**。
可训练的时候我们手上只有训练集，模型又完全有机会把训练集"背下来"（把函数扭曲成刚好穿过每一个训练样本的形状），然后在真正的新数据上一塌糊涂。

这就是**[[过拟合]]**。

## 怎么知道有没有过拟合：两套卷子

[[数据集]] 里已经分好了三份数据，这里正好派上用场：

| | 比喻 | 用途 |
|---|---|---|
| 训练集 | 平时作业 | 用来[[更新参数]] |
| 验证集 | 模拟考 | 开发过程中反复看，调旋钮 |
| 测试集 | 期末考试 | 最后只看一次 |

**训练集 loss 和验证集 loss 之间的差距，就是过拟合的体温计。**

- 两边**差不多、但都下不去** → 欠拟合。模型太笨了，压根没学到东西，该扩建网络
- 训练集**明显低于**验证集 → 过拟合。训练集上的好成绩是背出来的

第二种情况在 makemore_2 的末尾就出现了：训练集 loss 还在缓慢下降，验证集却开始跟不上了——那就是过拟合的预兆

## 正则化：给模型的自由度上锁

过拟合的根源是模型**太自由**了：参数量那么大，它总能把训练集里的每一个巧合、每一点噪声都当成规律记下来

所以正则化的思路很朴素：**在"答对题"这个目标之外，再加一条"别太自由"的要求**

论文里的写法是给原来的目标加一个惩罚项：

$$\text{目标} = \text{原来的 loss} + \alpha \cdot R(\theta)$$

"正则"这两个字的意思就是"让模型规矩一点"。

## 最常用的那把锁：权重衰减

代码就多了一行：

```python
loss = -probs[torch.arange(num), ys].log().mean() + 0.01 * (W**2).mean()
```

（makemore_1 里第一次见到它；论文里也是这么做的，叫"惩罚参数的平方范数"，和岭回归 ridge regression 是一回事）

它的意思是：**除了要求模型答对，还额外要求 W 里每个数都尽量小。**

### 为什么这样就能压住 W

把它拆开看。多加的这一项是 $\frac{\alpha}{n}\sum_i W_i^2$（$n$ 是 W 里元素的个数），它对 W 的 [[grad]] 贡献是：

$$\frac{\partial}{\partial W_i}\left(\frac{\alpha}{n}\sum_j W_j^2\right) = \frac{2\alpha}{n}W_i$$

于是 [[更新参数]] 的时候：

$$W \leftarrow W - \text{lr}\cdot\left(\text{grad}_{\text{原来}} + \frac{2\alpha}{n}W\right) = \underbrace{\left(1 - \frac{2\alpha\,\text{lr}}{n}\right)W}_{\text{先缩一点水}} - \text{lr}\cdot\text{grad}_{\text{原来}}$$

**每一步更新，W 都会先被乘上一个略小于 1 的系数**——这就是"权重衰减"这个名字的来历。

它不是在"把 W 推向 0"，而是**每一步让 W 缩水一点点**，和"想让 loss 变小"的那股梯度相互拉扯。W 最后停在哪儿，是这两股力平衡的结果。

### 拿你笔记本里的数算一遍

makemore_1 里是 W 有 729 个数、lr = 50、α = 0.01，那么每步的缩水系数是：

$$1 - \frac{2 \times 0.01 \times 50}{729} \approx 0.9986$$

单看一步只有 0.14% 的变化，几乎看不出来。但训 200 步累积下来：

$$0.9986^{200} \approx 0.76$$

也就是还剩下原来的 76%

把 α 提到 0.1（正则化强度提高十倍），系数变成 $1 - 0.0137 \approx 0.9863$，200 步之后只剩：

$$0.9863^{200} \approx 0.063$$

**6.3%。** 这就解释了笔记本里那个现象：α 一提，`W.max()` 从 4.26 一路变小

顺带一提，这时代价也出现了——**loss 效果没有之前好，但 W 的最大值变小了**。这不是 bug，**正则化本来就是拿一点训练集上的表现去换泛化。**

## α 是个旋钮

| α | 效果 |
|---|---|
| 0 | 不加正则，模型完全自由 |
| 小（0.01） | 轻轻压一压 |
| 大（0.1） | 压得很凶，训练集 loss 明显变差 |

所以它和 [[学习率]]、`n_embd` 一样，是"炼丹"时要调的旋钮之一。**太弱了防不住过拟合，太强了会把模型勒死。**

## 为什么"W 小"就等于"不容易过拟合"

回忆 [[神经元]] 的 $y = Wx + b$：

- **W 越大**，输入 x 变一点点，输出就跟着变一大截——函数很"陡"
- 训练集里总带着噪声和巧合。函数陡的时候，模型会把这些噪声也放大、当成规律学进去
- **W 小** → 函数更平缓 → 对输入的微小扰动不敏感 → 更容易学到真正稳定的规律（这就是[[鲁棒性]]）

一句话：**小权重让模型"不那么敏感"，而不敏感正是泛化的前提。**

## 有一类参数不该被惩罚

论文专门提了一句：权重衰减只加在权重上，**不加在偏置 b 上**。

为什么 b 不一样？

- b 只是给这一层的输出统一加一个偏移，**它不放大输入的变化**
- 能让函数"陡"起来的是 W，b 做不到
- 所以 b 取多大，都不影响"模型有多敏感"，惩罚它只是白白牺牲表达能力

论文还提醒了一个容易踩的坑：**如果对 W 做正则、却对嵌入层 C 不做，理论上 W 会趋向 0 而 C 会爆炸**——因为模型可以靠把 C 放大来补偿 W 的缩小，绕开你的正则。所以 C 得跟 W 一起惩罚。

## 正则化不只有权重衰减

| 方法 | 思路 |
|---|---|
| 权重衰减 | 惩罚大的参数，把模型压平 |
| 更多数据 | 让"背题"变得不划算——题多到背不完 |
| 把网络改小 | 直接减少自由度 |
| [[激活函数]]/BatchNorm | BatchNorm 本意是稳定训练（让 hpreact 近似标准正态分布），但它是按 batch 的统计量归一化的，同 batch 的样本互相影响，相当于引入了噪声，顺带就有正则效果 |
| Dropout | 训练时随机"关掉"一部分神经元，逼模型不依赖任何单个神经元（这一系列里没讲，但很常用） |

共同点只有一个：**它们都在限制模型去迎合训练集的自由度。**

## 总结

| 问题 | 答案 |
|---|---|
| 为什么要正则化 | 因为 loss 小 ≠ 学得好，模型会变成背题机器 |
| 怎么发现过拟合 | 看训练集和验证集 loss 的差距——那就是体温计 |
| 正则化在做什么 | 在"答对题"之外，额外要求模型"别太自由" |
| 权重衰减怎么写 | `loss + α * (W**2).mean()` |
| 它为什么叫"衰减" | 化简后每步更新会把 W 乘上一个略小于 1 的系数 |
| α 是什么 | 正则化强度，是个旋钮；大了训练集变差，小了防不住过拟合 |
| 为什么 W 小就更稳 | W 小 → 函数平缓 → 对输入扰动不敏感 → 泛化 |
| 为什么不惩罚 b | b 只是偏移，不放大输入变化，惩罚它只会白白削弱表达能力 |
| 为什么不惩罚 C | 要惩罚！否则模型会靠放大 C 来补偿 W 的缩小，绕开正则 |
| 本质是什么 | 拿一点训练集表现，换泛化 |

一句话：**正则化就是在"答对训练集"和"别太敏感"之间做权衡，权重衰减是最简单的那把锁。**
