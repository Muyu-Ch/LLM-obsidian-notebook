---
tags:
  - AI/语言模型
---

[[映射表]]给每个字符发了一个编号：`.` = 0，`a` = 1，`b` = 2……

但编号是个数字，而[[神经网络]]拿到数字就会**当成数字**来算
于是 `a`(1) 和 `b`(2) 天然离得近，`a`(1) 和 `z`(26) 天然离得远

这个远近是我们排字母表排出来的，不是数据说的
独热就是对付这件事的第一招

## 做法：把编号摊开

比如编号 13，独热会把它变成一个27维向量：**第13位是1，其余26位全是0**

```python
F.one_hot(xs)                                   # 直接用
xenc = F.one_hot(xs, num_classes=27).float()    # makemore_1 里的写法
```

## 有两个地方必须手动写

看上面第二行，有两个参数不是默认的：

### num_classes = 27

不写的话，torch 会**自己去猜**要开几个维度——猜法是"取这批数据里最大的编号，再加1"

问题是：这一批数据里不一定每个字符都出现过。要是这批里最大的编号恰好是25，那就只得到26维，可我们的词表其实有27个字符（`.` = 0 一直到 `z` = 26）

**维度必须由词表大小决定，不能由"这批数据里恰好出现了什么"决定。**

### .float()

torch 的 one_hot 默认输出整数类型
但后面它要参与矩阵乘法、要被[[反向传播]]求 [[grad]]，必须是浮点数

## 它解决了什么

27个字符变成27根互不相干的轴。每个字符站在自己的轴上，谁跟谁都没有天然的距离

也就是说：**我们不再替数据表态"e 和 f 有点像"——把"它们之间是什么关系"这个问题整个留空**，等[[训练]]的时候让数据自己说

## 但代价是：关系被清成了零

独热向量两两**正交**：任意两个不同的独热向量，内积是 0，夹角 90°

$$e \cdot f = (0,0,1,0) \cdot (0,1,0,0) = 0$$

（两个向量的1落在不同的位上，同一位永远不会同时是1，所以逐位相乘再相加，每一项里都至少有一个0）

所以独热只是把"假的关系"换成了"没有关系"。它没有骗人，但也没有给网络留下任何**发现**相似性的余地

## 而且有一半算力是浪费的

27维里只有1个1，剩下26维全是0

```python
logits = xenc @ W      # W 是 (27, 27)
```

这个乘法要算27次乘加，其中26次在算"0乘以一个数"，等于什么都没干

换个角度看就更清楚了：乘出来的结果其实**就是 W 的第13行**——因为除了第13位，其他位乘出来都是0，加到最后什么都不剩

**独热 @ W 的数学本质，就是"取出 W 的某一行"。** 这个观察直接通向[[嵌入层]]：既然结果就是取一行，那不如干脆别做这个乘法，直接查表

## 回头看 makemore_1

独热在 makemore_1 里是这么用的：

```python
W = torch.randn((27, 27), generator=g, requires_grad=True)
xenc = F.one_hot(xs, num_classes=27).float()

logits = xenc @ W
counts = logits.exp()
probs = counts / counts.sum(1, keepdim=True)
```

训出来的 W，和手动数出来的 bigram 计数表几乎一模一样（笔记本里对比过这两者）

现在可以理解为什么了：**W 的第 i 行，就是"编号为 i 的字符后面接什么"的那27个数**——和计数表里的第 i 行是同一个东西

## 总结

| 问题 | 答案 |
|---|---|
| 独热在干什么 | 把编号 i 变成一个长度等于词表大小的向量，只有第 i 位是1 |
| 它解决的是什么 | 消除编号带来的假远近关系——字符之间不再有"天生"的距离 |
| 但留下了什么 | 所有独热向量两两正交，关系被清成了零 |
| 为什么浪费 | 一整串0去乘矩阵，而且结果其实就是"取出某一行" |
| 通向哪里 | [[嵌入层]]：直接查表，让网络自己学出字符之间的远近 |
| 为什么 num_classes 要手写 | 否则 torch 按这批数据的最大值猜，维度可能不到词表大小 |
| 为什么 .float() | one_hot 默认是整数，参与训练需要浮点数 |

一句话：**独热是"把关系留空"，[[嵌入层]]是"让数据把关系填上"。**
