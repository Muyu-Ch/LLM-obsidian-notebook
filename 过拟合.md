---
tags:
  - AI/训练
---

[[训练]]的直觉是"[[loss]] 越小越好"
但 makemore 练到后半程会撞上一件反直觉的事：
**loss 还能继续降，可模型反而越来越没用。**

## 一个具体的场景

makemore_1 里，W 逐渐逼近理论最优解，loss 停在 2.46 左右再也下不去
那个模型确实不怎么样。于是到了 makemore_2，我们的做法是**把网络扩建**——更多神经元、更宽的嵌入向量

结果 loss 果然降了。但如果我们一路降下去，降到接近 0 会怎样？

> 模型只会吐出正确答案，成为了背题机器而不是学习机器。

这就是**过拟合**：

> **模型把训练集"背"下来了，而不是"学"到了规律。**

注意这里的区别不是"答对多少"，而是"**怎么答对的**"。

## 用学生做题来想

这个概念用考试比喻最清楚：

| | 平时作业（训练集） | 新题（新数据） |
|---|---|---|
| **背答案的学生** | 全对 | 崩 |
| **认真学的学生** | 不一定满分 | 也能做 |

背答案的学生把每道题的答案都记住了，但他**没有学会解题**——题目稍微一变形就露馅

模型是一样的。它完全有能力把训练集里的**每一个样本**都记住，因为[[神经网络]]的自由度太高了：参数那么多，总能把函数扭曲成刚好穿过每一个训练样本的形状。

而训练集里**总带着噪声和巧合**——数据是从现实里采来的，不是精心设计的。区分"规律"和"巧合"是需要付出代价的，如果模型足够自由，它没必要区分：**两个都记住，训练集 loss 就能更低。**

问题的根源在这里：

> **训练集 loss 这个指标，从来不惩罚"背题"这个行为。它只问"你答对了吗"，不问"你是怎么答对的"。**

## 所以要有"新题"

因为我们没法直接看见"模型有没有在背题"，只能**造一份新数据来对照**。这正是 [[数据集]] 里把数据分成三份的意义：

- 训练集是平时作业，用来[[更新参数]]
- 验证集是**模拟考**——模型没见过，所以测出来的分数才有真实性

> 就好比我们考试的时候，如果直接给平时作业原题那就没有效果了，只有出一些新题考学生才行。

## 体温计：两边的差距

**训练集 loss 和验证集 loss 之间的差距，就是过拟合的体温计。**

因为这两份数据是错开的，模型的"真实水平"只能靠验证集看：

- **两边差不多，但都下不去** → 欠拟合
- **训练集明显低于验证集** → 过拟合

把三种状态摆在一起：

| 状态 | 训练集 loss | 验证集 loss | 差距 | 模型在干什么 |
|---|---|---|---|---|
| 欠拟合 | 高 | 高 | 小 | 太笨，规律都没学会 |
| 刚刚好 | 低 | 低 | 小 | 学到了规律 |
| 过拟合 | 很低 | 高 | **大** | 把训练集背下来了 |

拿你笔记本里的实测对照一下：

1. 最早那个 100 个神经元的网络——**"训练集和验证集的 loss 太接近了"**，但两个都不够低 → 欠拟合，模型"笨笨的"，不太能学会东西
2. 于是扩建到 300 个神经元、把嵌入向量从 2 维加到 10 维 → 验证集 loss 突破 2.3
3. 继续练下去 → **训练集 loss 还在缓慢下降，验证集却跟不上了**，说明模型达到极限，开始出现过拟合预兆

## 那条 U 形曲线

如果边训练边把验证集 loss 画出来，它**不是一路下降**的：

```
验证集 loss
    │＼
    │  ＼            ← 模型在学规律
    │    ＼
    │      ＼___
    │          ＼___  ← 最低点：该停了
    │               ＼
    │                 ＼__  ← 开始背题，越背越糟
    └──────────────────────► 训练步数
```

- **前期**：验证集跟着训练集一起降——模型确实在学规律
- **拐点**：验证集 loss 降到最低
- **之后**：验证集开始**上升**，而训练集还在降——模型已经把规律学完了，剩下的时间全花在背噪声上

那个拐点就是"该停手"的地方。

这条 U 形曲线里藏着最核心的矛盾：

> **我们只能拿训练集去调[[参数]]，但真正关心的是验证集。这两条曲线只在前期是一致的。**

## 两个方向，都是"容量"问题

回头看，欠拟合和过拟合的解法正好相反：

| 状态 | 病因 | 药方 |
|---|---|---|
| 欠拟合 | 模型容量 < 数据里的规律 | **加大**：更多神经元、更宽的嵌入、更多层 |
| 过拟合 | 模型容量 > 数据能支撑的 | **减小**：改小、加[[正则化]]、或者加数据 |

所以"扩建网络"和"正则化"是**两个方向相反的旋钮**，而"炼丹"就是在这中间找到那个点。

## 和正则化的分工

- **过拟合是症状，[[正则化]]是药。**
- 药不止一种（权重衰减、更多数据、BatchNorm……见[[正则化]]里的表），但它们干的是同一件事：**限制模型去迎合训练集的自由度**
- 还有一个容易懵的地方：**加了正则化之后，训练集 loss 变差是正常的，甚至是目的**

因为正则化就是拿一点训练集上的表现，去换泛化。这也再一次说明——**训练集 loss 从来不是我们真正要优化的东西。**

## 总结

| 问题 | 答案 |
|---|---|
| 过拟合是什么 | 模型把训练集背下来了，而不是学到规律 |
| 为什么只看 loss 发现不了 | loss 只问"答对没有"，不问"是怎么答对的" |
| 怎么诊断 | 训练集和验证集 loss 的差距——那就是体温计 |
| 差距小但都高 | 欠拟合，模型太笨，该扩建 |
| 差距大 | 过拟合，该正则化 / 加数据 / 改小 |
| 训练时要注意什么 | 验证集 loss 是 U 形的，拐点之前该停手 |
| 核心矛盾 | 参数只能用训练集调，但我们真正关心的是验证集 |
| 和正则化什么关系 | 过拟合是症状，正则化是药；治它的代价就是训练集 loss 变差 |

一句话：**过拟合是"把训练集背下来了"，所以只盯训练集 loss 永远发现不了它——得拿一份新数据对照着看。**
